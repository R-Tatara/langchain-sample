[project]
name = "langchain-sample"
version = "0.1.0"
description = "LangChain sample project exploring LLM agent patterns with Google Gemini"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.11",
    "langchain-google-genai>=4.2.6",
    "pydantic>=2.13.4",
]
license = { text = "MIT" }